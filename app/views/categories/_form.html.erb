<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3" style="margin-top:20px;">
    <%= form.label :name_your_category, style: "display: block",  class: "form-label"%>
    <%= form.text_field :name, style: "width: 50%", class: "form-control", placeholder: "Sport" %>
  </div>

  <div class="mb-3">
    <%= form.label :description, style: "display: block",  class: "form-label" %>
    <%= form.text_field :description,  style: "width: 50%", class: "form-control", placeholder: "Gloves for football"%>
  </div>

  <div class="mb-3">
    <%= form.submit class: "btn btn-outline-secondary" %>
  </div>
<% end %>


